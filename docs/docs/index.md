---
sidebar_position: 1
---

# Introduction

![Docusaurus themed image](/img/ultron_banner_light.png#gh-light-mode-only)![Docusaurus themed image](/img/ultron_banner_dark.png#gh-dark-mode-only)

Ultron is a UI testing framework for **Android** and **Compose Multiplatform**. It is built on top of Espresso, Espresso Web, UI Automator and the Compose UI testing API — you don't need to learn new classes or a special syntax, and every action and assertion becomes stable, logged and reportable for free.

## Supported platforms

| Target | Library | Testing API under the hood |
|---|---|---|
| Android (views) | `ultron-android` | Espresso, Espresso Web, UI Automator |
| Android (Compose) | `ultron-compose` | Compose UI testing |
| iOS | `ultron-compose` | Compose UI testing |
| Desktop (JVM) | `ultron-compose` | Compose UI testing |
| Web (JS & wasmJs) | `ultron-compose` | Compose UI testing |
| macOS | `ultron-compose` | Compose UI testing |

Allure report artifacts are generated by `ultron-allure` and are available for Android UI tests only.

## Which library do I need?

- **`com.atiurin:ultron-compose`** — Compose UI tests. Works both for an Android application and for Compose Multiplatform targets.
- **`com.atiurin:ultron-android`** — native Android UI tests based on Espresso (including the web part) and UI Automator.
- **`com.atiurin:ultron-allure`** — Allure report support for Android UI tests.

They can be used together: a single Android test may mix Espresso, UI Automator and Compose operations. See [Connect to project](intro/connect.md) for the dependency snippets.

## Your first test

### Compose Multiplatform

Replace `runComposeUiTest` with `runUltronUiTest` and interact with a `SemanticsMatcher` directly — no test rule to pass around:

```kotlin
class ExampleTest {
    @Test
    fun myTest() = runUltronUiTest {
        setContent {
            App()
        }
        hasTestTag("text").assertTextEquals("Hello")
        hasTestTag("button").click()
        hasTestTag("text").assertTextEquals("Compose")
    }
}
```

Read more in [Compose Multiplatform](compose/multiplatform.md).

### Android

Create an Ultron compose rule (for Compose content) and call operations on matchers anywhere in your code:

```kotlin
class ComposeTest {
    @get:Rule
    val composeRule = createDefaultUltronComposeRule()

    @Test
    fun myTest() {
        composeRule.setContent { App() }

        hasTestTag("Continue").click()
        hasText("Welcome").assertIsDisplayed()
    }
}
```

For Espresso and UI Automator no rule is needed at all:

```kotlin
withId(R.id.send_button).isDisplayed().click()   // Espresso
byResId(R.id.button1).click()                    // UI Automator
```

Read more in [Compose for Android](compose/android.md), [Espresso](android/espress.md) and [UI Automator](android/uiautomator.md).

## A few words about syntax

The standard syntax provided by Google is intricate and not intuitive. Compare:

_Compose UI testing API_

```kotlin
composeTestRule.onNode(hasTestTag("Continue")).performClick()
composeTestRule.onNodeWithText("Welcome").assertIsDisplayed()
```

_Ultron_

```kotlin
hasTestTag("Continue").click()
hasText("Welcome").assertIsDisplayed()
```

The difference grows with the complexity of the interaction. A single item of a Compose list:

_Compose UI testing API_

```kotlin
val itemMatcher = hasText(contact.name)
composeRule
    .onNodeWithTag(contactsListTestTag)
    .performScrollToNode(itemMatcher)
    .onChildren()
    .filterToOne(itemMatcher)
    .assertTextContains(contact.name)
```

_Ultron_

```kotlin
composeList(hasTestTag(contactsListTestTag))
    .item(hasText(contact.name))
    .assertTextContains(contact.name)
```

More comparisons — Espresso, RecyclerView, Espresso Web and UI Automator — are on the [project home page](https://open-tool.github.io/ultron/).

## Why are all Ultron actions and assertions more stable?

The framework captures a list of specified exceptions and repeats the operation during a timeout period (5 seconds by default). An exception outside that list is not retried — the test fails immediately.

```kotlin
withId(R.id.result).withTimeout(10_000).hasText("Passed")
```

The list of handled exceptions, timeouts and the result analyzer are all configurable — see [Configuration](intro/configuration.md). You can also assert that an operation actually had an effect and repeat it otherwise, see [custom assertions](common/customassertion.md).

Any operation result is available as a `Boolean` without try/catch:

```kotlin
val isButtonDisplayed = withId(R.id.button).isSuccess { withTimeout(2_000).isDisplayed() }
if (isButtonDisplayed) {
    //do some reasonable actions
}
```

## 3 steps to develop a test using Ultron

We advocate for a proper test framework architecture, division of responsibilities between layers, and other best practices. Therefore, when using Ultron, we recommend the following approach:

1. Create a Page Object and specify screen UI elements as properties.

```kotlin
object ChatPage : Page<ChatPage>() {
    private val messagesList = withId(R.id.messages_list)
    private val clearHistoryBtn = withText("Clear history")
    private val inputMessageText = withId(R.id.message_input_text)
    private val sendMessageBtn = withId(R.id.send_button)
}
```

It's recommended to make all Page Objects as `object` and descendants of `Page` class.
This allows for the utilization of convenient Kotlin features. It also helps you to keep Page Objects stateless.

2. Describe user step methods in Page Object.

```kotlin
object ChatPage : Page<ChatPage>() {
    fun sendMessage(text: String) = apply {
        inputMessageText.typeText(text)
        sendMessageBtn.click()
        getMessageListItem(text).text
             .isDisplayed()
             .hasText(text)
    }

    fun clearHistory() = apply {
        openContextualActionModeOverflowMenu()
        clearHistoryBtn.click()
    }
}
```
Refer to the full code sample [ChatPage.kt](https://github.com/open-tool/ultron/blob/master/sample-app/src/androidTest/java/com/atiurin/sampleapp/pages/ChatPage.kt)

3. Call user steps in test

```kotlin
    @Test
    fun friendsItemCheck(){
        FriendsListPage {
            assertName("Janice")
            assertStatus("Janice","Oh. My. God")
        }
    }
    @Test
    fun sendMessage(){
        FriendsListPage.openChat("Janice")
        ChatPage {
            clearHistory()
            sendMessage("test message")
        }
    }
```
Refer to the full code sample [DemoEspressoTest.kt](https://github.com/open-tool/ultron/blob/master/sample-app/src/androidTest/java/com/atiurin/sampleapp/tests/espresso/DemoEspressoTest.kt)

In essence, your project's architecture will look like this:

![architecture](https://github.com/open-tool/ultron/assets/12834123/b0882d34-a18d-4f1f-959b-f75796d11036)

When a UI element belongs to a repeating container — a contact card, a list row — describe it once as a [UI Block](common/uiblock.md) instead of locating its children across the whole screen.

## Where to go next

**Setup**

- [Connect to project](intro/connect.md) — dependencies for Android and Multiplatform
- [Dependencies management](intro/dependencies.md) — what comes transitively
- [Configuration](intro/configuration.md) — timeouts, allowed exceptions, listeners

**Writing tests**

- [Compose](compose/index.md), [Compose LazyList](compose/lazylist.md) and the full [Compose API](compose/api.md)
- [Espresso](android/espress.md), [RecyclerView](android/recyclerview.md), [WebView](android/webview.md), [UI Automator](android/uiautomator.md)
- [UI Block](common/uiblock.md) — elements in the scope of their parent container
- [UltronTest](common/ultrontest.md) — preconditions and postconditions, per class and per single test, plus soft assertions
- [Test conditions management](android/testconditions.md) — `RuleSequence`, `SetUpRule`, `TearDownRule`

**Reporting and extension**

- [Allure report](common/allure.md) — steps, screenshots and view hierarchy out of the box
- [Listeners](common/listeners.md) — hook into every stage of an operation
- [Ultron extension](common/extension.md) — add your own operations
- [Custom assertions](common/customassertion.md) and [Result handler](common/resulthandler.md)

## Allure report

Ultron has built in support to generate artifacts for Allure reports. Just apply the recommended configuration and set `testInstrumentationRunner`.

For the complete guide, refer to the [Allure description](./common/allure.md)

```kotlin
@BeforeClass @JvmStatic
fun setConfig() {
    UltronConfig.applyRecommended()
    UltronAllureConfig.applyRecommended()
    UltronComposeConfig.applyRecommended() 
}
```
![allure](https://github.com/open-tool/ultron/assets/12834123/c05c813a-ece6-45e6-a04f-e1c92b82ffb1)

![allure compose](https://github.com/open-tool/ultron/assets/12834123/1f751f3d-fc58-4874-a850-acd9181bfb70)
